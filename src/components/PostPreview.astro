---
const {title, publishDate, author, description, slug} = Astro.props;
---

<style>
    /*
    *   This really cool underline effect was taken from:
    *   https://tobiasahlin.com/blog/css-trick-animating-link-underlines/
    */
    a {
        position: relative;
        color: #1D1128;
        text-decoration: none;
        font-family: 'Fira Sans', sans-serif;
        font-size: 1.4rem;
        font-weight: bold;
    }

    a:hover {
        color: #1D1128
    }

    a::before {
        content: "";
        position: absolute;
        display: block;
        width: 100%;
        height: 2px;
        bottom: 0;
        left: 0;
        background-color: #000;
        transform: scaleX(0);
        transition: transform 0.3s ease;
        }

    a:hover::before {
        transform: scaleX(1);
    }
</style>

<li>
    <a href={`/posts/${slug}/`}>{title}</a>
    <p>{author}</p>
    <time>{new Date(publishDate).toLocaleString()}</time>
    <p>{description}</p>
</li>